// This is the datatype to contain Config data. Should only 
// contain data to be transferred form step to step
export interface ComponentConfig {
    email: string;
    password: string;
    M2MGO_Entity: string;
    Entity_key: string;
}
